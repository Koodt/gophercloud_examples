# Example configuration file for OpenStack API access
# Copy this file to config.yaml and fill in your actual credentials
# DO NOT commit config.yaml to version control

auth_url: "http://127.0.0.1:8080/identity/v3"
username: "admin"
password: "strong_password"
project_name: "admin"
domain_name: "default"
region: "RegionOne"

